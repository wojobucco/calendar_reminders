<!DOCTYPE html>
<html>
<head>
  <title>CalendarReminders</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="top_nav">
    <%= link_to "Home", root_path %>
  </div>
  <div id="auth">
    <% if current_user %>
      Hello <%= current_user.email %> | <%= link_to "Sign out", sessions_destroy_path, {method: 'delete' } %>
    <% else %>
      <%= link_to image_tag('google/Red-signin-Long-base-44dp.png'), new_session_path %>
    <% end %>
  </div>
<%= yield %>

</body>
</html>
