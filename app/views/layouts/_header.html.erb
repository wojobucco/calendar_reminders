<header>
  <nav class="navbar navbar-default " role="navigation">
    <ul class="nav navbar-nav">
      <li><%= link_to "Home", root_path %></li>
      <li><%= link_to "Company", "#{root_url}info/about" %></li>
      <li><%= link_to "Contact", "#{root_url}info/contact" %></li>
    </ul>
    <div id="auth" class="pull-right">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li><%= link_to "Profile", "#{root_url}profile/#{current_user.id}" %></li> 
          <li><%= link_to "Sign out", sessions_destroy_path, {method: 'delete' } %></li>
        <% else %>
          <li><%= link_to_function "Sign in", "$('#myModal').modal()" %></li>
          <li><%= link_to_function "Register", "$('#myModal').modal()" %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>
